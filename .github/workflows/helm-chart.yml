name: Helm Chart

on:
  push:
    branches:
      - main
      - develop
      - authmind-main
    paths:
      - 'infra/helm/**'
      - 'infra/database/init/**'
      - '.github/workflows/helm-chart.yml'
  pull_request:
    paths:
      - 'infra/helm/**'
      - 'infra/database/init/**'
      - '.github/workflows/helm-chart.yml'

jobs:
  lint:
    name: Lint and Template
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up Helm
        uses: azure/setup-helm@v5
        with:
          version: v3.15.2

      - name: Verify db-init SQL copies are in sync
        run: |
          # The chart bundles a copy of infra/database/init/*.sql under
          # infra/helm/vigil/files/database-init/ because Helm can only read files
          # inside the chart directory. Drift here means the chart would
          # ship a different schema than the docker-compose stack.
          diff -r infra/database/init infra/helm/vigil/files/database-init \
            --exclude=README.md

      - name: Fetch chart dependencies
        run: |
          helm repo add bitnami https://charts.bitnami.com/bitnami
          helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts
          helm dependency update infra/helm/vigil
          # Helm 4 requires subcharts extracted, not just as tgz
          for tgz in infra/helm/vigil/charts/*.tgz; do
            tar -xzf "$tgz" -C infra/helm/vigil/charts/
          done

      - name: helm lint (default values)
        run: helm lint infra/helm/vigil

      - name: helm lint (dev values)
        run: helm lint infra/helm/vigil -f infra/helm/vigil/values-dev.yaml

      - name: helm lint (Bitnami subcharts enabled)
        run: |
          helm lint infra/helm/vigil \
            --set postgresql.enabled=false --set postgresql.bitnami.enabled=true \
            --set redis.enabled=false --set redis.bitnami.enabled=true

      - name: helm template (default values)
        run: |
          helm template release-check infra/helm/vigil \
            --set secrets.anthropicApiKey=test-key \
            --set secrets.postgresPassword=test-password \
            > /tmp/manifest-default.yaml
          echo "Rendered $(grep -c '^kind:' /tmp/manifest-default.yaml) resources."

      - name: helm template (dev values)
        run: |
          helm template release-check infra/helm/vigil \
            -f infra/helm/vigil/values-dev.yaml \
            --set secrets.anthropicApiKey=test-key \
            > /tmp/manifest-dev.yaml

      - name: helm template (Bitnami subcharts)
        run: |
          helm template release-check infra/helm/vigil \
            --set secrets.anthropicApiKey=test-key \
            --set postgresql.enabled=false --set postgresql.bitnami.enabled=true \
            --set redis.enabled=false --set redis.bitnami.enabled=true \
            > /tmp/manifest-bitnami.yaml
          echo "Rendered $(grep -c '^kind:' /tmp/manifest-bitnami.yaml) resources."

      - name: Validate manifests with kubeconform
        run: |
          curl -sSLo kubeconform.tar.gz \
            https://github.com/yannh/kubeconform/releases/download/v0.6.7/kubeconform-linux-amd64.tar.gz
          tar xzf kubeconform.tar.gz
          ./kubeconform -strict -ignore-missing-schemas -summary /tmp/manifest-default.yaml
          ./kubeconform -strict -ignore-missing-schemas -summary /tmp/manifest-dev.yaml
          ./kubeconform -strict -ignore-missing-schemas -summary /tmp/manifest-bitnami.yaml

  chart-testing:
    name: chart-testing
    runs-on: ubuntu-latest
    needs: lint
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Helm
        uses: azure/setup-helm@v5
        with:
          version: v3.15.2

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.11"
          check-latest: true

      - name: Set up chart-testing
        uses: helm/chart-testing-action@v2.8.0

      - name: Add chart repositories
        # `ct lint` runs `helm dependency build` under the hood, which
        # requires the upstream repos in `Chart.yaml` to be registered.
        # Repo additions don't carry across jobs, so we add them here
        # too (the lint job above adds the same set).
        run: |
          helm repo add bitnami https://charts.bitnami.com/bitnami
          helm repo add open-telemetry https://open-telemetry.github.io/opentelemetry-helm-charts
          helm repo update

      - name: Run chart-testing (lint)
        run: ct lint --check-version-increment=false --target-branch ${{ github.event.repository.default_branch }} --chart-dirs infra/helm
