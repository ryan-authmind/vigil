# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so
node_modules/
# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Translations
*.mo
*.pot

# Django stuff:
*.log
local_settings.py
dbite3
db.sqlite3-journal

# Flask stuff:
instance/
.webassets-cache

# Scrapy stuff:
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
.pybuilder/
target/

# Jupyter Notebook
.ipynb_checkpoints

# IPython
profile_default/
ipython_config.py

# pyenv
#   For a library or package, you might want to ignore these files since the code is
#   intended to run in multiple environments; otherwise, check them in:
# .python-version

# pipenv
#   According to pypa/pipenv#598, it is recommended to include Pipfile.lock in version control.
#   However, in case of collaboration, if having platform-specific dependencies or dependencies
#   having no cross-platform support, pipenv may install dependencies that don't work, or not
#   install all needed dependencies.
#Pipfile.lock

# UV
#   Similar to Pipfile.lock, it is generally recommended to include uv.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#uv.lock

# poetry
#   Similar to Pipfile.lock, it is generally recommended to include poetry.lock in version control.
#   This is especially recommended for binary packages to ensure reproducibility, and is more
#   commonly ignored for libraries.
#   https://python-poetry.org/docs/basic-usage/#commit-your-poetrylock-file-to-version-control
#poetry.lock

# pdm
#   Similar to Pipfile.lock, it is generally recommended to include pdm.lock in version control.
#pdm.lock
#   pdm stores project-wide configurations in .pdm.toml, but it is recommended to not include it
#   in version control.
#   https://pdm.fming.dev/latest/usage/project/#working-with-version-control
.pdm.toml
.pdm-python
.pdm-build/

# PEP 582; used by e.g. github.com/David-OConnor/pyflow and github.com/pdm-project/pdm
__pypackages__/

# Celery stuff
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.env.local
# Timestamped .env backups written by local tooling. One was sitting untracked
# with a live provider key in it, which `git add -A` would have published.
.env.bak.*
.env.*.bak
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# PyPI configuration file
.pypirc

# IDEs
.vscode/
.idea/
.cursor/
.claude/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.pid

# Secrets
*.pem
*.key
config.json
*_config.json
!mcp-config.json
!infra/docker/bifrost/config.json

# Bifrost's runtime state, written when the gateway runs outside Docker.
# config.db is its config store and holds provider API keys -- in plaintext
# unless the deployment turned on Bifrost's key encryption; logs.db carries
# request and response bodies. Neither is ever committed.
bifrost/
/logs.db

# Node.js / React
clients/web/node_modules/
clients/web/build/
clients/web/dist/
clients/web/.env
clients/web/.env.local
clients/web/.env.development
clients/web/.env.development.local
clients/web/.env.test.local
clients/web/.env.production.local
clients/web/npm-debug.log*
clients/web/yarn-debug.log*
clients/web/yarn-error.log*

# Build artifacts
*.pyd
*.exe
*.dll

# Test outputs
TestOutputs/*.pdf
!TestOutputs/.gitkeep

# Timesketch
timesketch/data/
timesketch/logs/
timesketch/upload/
timesketch/etc/

# Temporary files
tmp/
temp/
*.tmp

# Runtime data (stored in PostgreSQL)
data/*.json
data/investigations/
!data/schemas/

# Test data
splunk_test_data.json
*_test.json
enrichment_result_*.json
claude_analysis_*.txt

# Autostart list, written by Settings -> Services (see services/autostart_config.py)
.vigil-autostart

# Local tooling artifacts / personal MCP config
.mcp.json
index.scip
index.scip.stamp
slop.toml
.zed/

# Bifrost's local gateway state: 81MB of SQLite at the repo root. Anchored --
# an unanchored `bifrost/` also swallows core/llm/bifrost/ and
# infra/docker/bifrost/, both of which are tracked source.
/bifrost/

# Where a file-ledger hunt writes its output. Real run data: sanitise it before it
# goes anywhere near the tree -- services/agent/scripts/sanitise-hunt-ledgers.ts.
# Left unanchored on purpose: a hunt run from services/agent/ writes its ledgers
# there, not at the repo root, and anchoring this to the root stopped ignoring
# them. The sanitised fixtures live under a runs/ too and are committed, so that
# one directory is negated -- git will not descend into an ignored directory, so
# the directory itself has to be unignored, not just the files in it.
runs/
!services/agent/tests/fixtures/runs/

# Editor, build and indexer scratch. All local, none of it ever committed.
.vite/
.zed/
index.scip.stamp

# The gateway's own store, wherever it is launched from: it holds provider API keys,
# in plaintext unless the deployment turned on Bifrost's key encryption.
/config.db
/logs.db
/bifrost/

# Working copies of two dropped submodules, which survive a checkout. Neither has a
# production importer here -- mcp-servers' four servers are vendored at tools/mcp/.
/deeptempo-core/
/mcp-servers/

# Local scratch space -- notes and throwaway files, never committed.
/.scratch/

# Repo-local uv install, created by scripts/lib.sh when the host has no uv.
/.uv/
